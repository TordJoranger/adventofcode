import java.io.File

fun fuel(x: Int) : Int {
   var y = x.div(3) -2
    return if(y > 8)
        y + fuel(y)
    else
        y
}

fun main() {
    File("input1.txt").forEachLine {
       val count = fuel(it.toInt())
    }
    println(count)
}